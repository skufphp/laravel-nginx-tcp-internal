# Laravel Boilerplate (PHP-FPM + Nginx TCP + PostgreSQL + Redis + Node.js)

Этот репозиторий представляет собой **boilerplate** для быстрого развертывания Laravel-проектов с оптимизированной архитектурой взаимодействия сервисов.

## 🚀 Особенности

*   **Производительность:** Связь между Nginx и PHP-FPM настроена через **TCP (порт 9000)**, что обеспечивает стабильную работу в Docker-окружении.
*   **Современный стек:** PHP 8.5 (Alpine), Nginx, PostgreSQL 18.2, Redis 8.6.
*   **Фронтенд (Node.js 24):** Отдельный контейнер для сборки ассетов и горячей перезагрузки (Vite HMR).
*   **Среды:** Разделение конфигураций для локальной разработки (`dev`) и продакшена (`prod`).
*   **Удобство разработки:** Готовый интерфейс **pgAdmin 4** для работы с БД.
*   **Автоматизация:** Мощный набор команд в `Makefile` для управления всеми аспектами проекта.
*   **Xdebug:** Предустановлен (через PIE) и готов к включению одной переменной в `.env`.

## 📂 Структура проекта

*   `docker/` — Docker-файлы и конфигурации для PHP, Nginx и PHP-FPM (`www.conf`).
*   `docker-compose.yml` — Базовая инфраструктура (PHP, Nginx, Postgres, Redis).
*   `docker-compose.dev.yml` — Расширение для разработки (Node.js, pgAdmin, проброс портов БД/Redis, volumes).
*   `docker-compose.prod.yml` — Конфигурация для продакшена (CI/CD образы, миграции, порты).
*   `Makefile` — Инструментарий для управления контейнерами и Laravel.
*   `SETUP.md` — Подробная инструкция по интеграции boilerplate в ваш проект.
*   `gitlab-ci.yml` — Пример CI/CD пайплайна.

## 🛠 Быстрый старт

Проект предназначен для интеграции в корень вашего Laravel-приложения.

1.  **Подготовка:** Создайте Laravel проект: `composer create-project laravel/laravel .`
2.  **Копирование:** Перенесите файлы boilerplate (`docker/`, `docker-compose*.yml`, `Makefile`, `SETUP.md`) в корень.
3.  **Настройка:** Создайте `.env` и настройте параметры (см. [SETUP.md](SETUP.md)).
4.  **Запуск:**
    ```bash
    make setup
    ```

### Основные команды Makefile:
*   `make up` / `make down` — запуск/остановка (Dev).
*   `make status` — проверка состояния контейнеров.
*   `make logs` — просмотр всех логов (есть также `logs-php`, `logs-nginx`, `logs-node` и т.д.).
*   `make artisan CMD="..."` — выполнение команд Artisan.
*   `make composer-install` / `make npm-install` — установка зависимостей.
*   `make info` — справочная информация о сервисах и портах.
*   `make validate` — проверка доступности сервисов по HTTP.

## 🌐 Доступы по умолчанию (Dev)
*   **Сайт:** [http://localhost](http://localhost)
*   **pgAdmin:** [http://localhost:8080](http://localhost:8080)
*   **PostgreSQL:** `localhost:5432` (с хоста)
*   **Redis:** `localhost:6379` (с хоста)
*   **Vite HMR:** `localhost:5173`

---
*Подробная инструкция по установке и настройке находится в файле [SETUP.md](SETUP.md).*
